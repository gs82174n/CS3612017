
public class tail {
	public static int recursion(int a, int b){
		if (a == 0)
			return b;
		else if (b == 0)
			return a;
		else 
			return (recursion(a-2, b + a));
	}
	
	public static void main(String[] args){
		System.out.println(recursion(2, 5));
	}
}
